{
  "name": "reservation-service",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "npx sequelize-cli db:migrate && node src/server.js",
    "dev": "npx sequelize-cli db:migrate && nodemon src/server.js",
    "test": "cross-env NODE_ENV=test TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/restaurant_test jest --runInBand"
  },
  "dependencies": {
    "amqplib": "^0.10.9",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^2.7.0",
    "pg": "^8.11.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.35.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "nodemon": "^3.0.1",
    "sequelize-cli": "^6.6.1",
    "supertest": "^7.1.4"
  }
}
